SERVICE="${SERVICE:-life-change}"
CONTAINER="${CONTAINER:-projects-life-change-1}"
IMAGE="${IMAGE:-life-change-life-change}"
ROUTE_PATH="${ROUTE_PATH:-/life-change/health}"
ROUTE_OK_CODES="${ROUTE_OK_CODES:-200}"
REGISTRY_IMAGE="${REGISTRY_IMAGE:-ghcr.io/goottjason/life-change}"
